<%- include ./partials/header.ejs %>

<div>
    <h4>My Current Location:</h4>
    <p id="currentlocation"><p>
    <button class="btn white-text blue" id="locationBtn">Get my location</button>
</div>

<!-- 
<hr><br>    
<div>
    <h4>Manually set latitude and longitude location</h4>
    <form action="/api/stopsAtLocation" method="GET"> 
        <input type="text" name="lat" placeholder="Latitude">
        <input type="text" name="lon" placeholder="Longitude">
        <button class="btn white-text blue" id="locationSetBtn">Set Location</button> 
        <input type="submit" class="btn white-text blue" value="Submit"> 
    </form> 
</div> 
-->

<hr><br>

<div>
    <h4>Find nearby stops:</h4>
    <button class="btn white-text blue" id="searchBtn">Find Nearby Stops</button>
    <table id="stopTable">
        <th>
            <td>Name</td>
            <td>Latitude</td>
            <td>Longitude</td>
        </th>
    </table>        
    <hr>
</div>
    




<% if(stops) { %> 
    <% stops.forEach(stop => { %>
        <p><%= stop.id %></p>
        <p><%= stop.name %></p>
        <p><%= stop.lat %></p>
        <p><%= stop.lon %></p>
        <!-- <% stop.routeIds.forEach(route => { %>
            <p><%= route %></p>
        <% }); %> -->
        <br>
    <% }); %>
<% } %>

<%- include ./partials/footer.ejs %>
